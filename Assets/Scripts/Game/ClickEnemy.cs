using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class ClickEnemy : MonoBehaviour
{/*
    public Camera mainCamera;  
    public ClickInputSystem inputSystem; 
    private int enemyLayerMask;  

    void Start()
    {
        enemyLayerMask = LayerMask.GetMask("Enemy");  // Enemy 레이어 마스크 초기화
    }

    void Update()
    {
        if (inputSystem.Click)
        {
            Ray ray = mainCamera.ScreenPointToRay(Mouse.current.position.ReadValue());
            RaycastHit hit;

            if (Physics.Raycast(ray, out hit, Mathf.Infinity, enemyLayerMask))
            {
                // Enemy 레이어 오브젝트를 클릭했을 때
                Debug.Log($"Clicked on Enemy: {hit.collider.gameObject.name}");
            }

            // 클릭 처리 후 Click 상태 초기화
            //inputSystem.ClickInput(false);
        }
    }
    */
}
